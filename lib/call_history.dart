import 'package:flutter/material.dart';

class call_history extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Call History'),
      )
      
    );
  }
}